# 2026-05-08

## 完成工作

### 修复出入库数量计算兼容性问题
- 问题：新版按区域管理库存使用 `数量_区域名` 字段，但旧数据只有 `数量` 字段，导致入库显示全0、入库后数量被覆盖
- 后端（app.py）：inbound/outbound API 增加旧数据检测，若无区域字段且存放区域匹配，自动迁移旧 `数量` 为区域数量
- 前端入库页（Inbound.vue）：增加 `hasAreaData()`、`getAreaQuantityDisplay()` 方法，兼容旧格式显示
- 前端出库页（Outbound.vue）：同样增加旧格式兼容，旧数据物资按其存放区域字段归组显示
